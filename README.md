some smart contracts for nas blockchain
==========================================

# Build

* `npm install`
* `npm install -g browserify`
* `browserify js/app.js -o js/app.bundle.js`

# 邮件合约 

* `smartContract/mail.js`

